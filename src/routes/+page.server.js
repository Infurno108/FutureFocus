export const actions = {
    submitPrompt: async (event) => {
        console.log('submitPrompt action executed server side');
    },
    regenerateList: async (event) => {
        console.log('regenerateList action executed server side');
    }
}